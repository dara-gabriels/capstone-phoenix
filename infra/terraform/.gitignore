*.tfvars
!*.tfvars.example
.terraform/
terraform.tfstate
terraform.tfstate.*
*.tfstate.backup
.terraform.lock.hcl
crash.log
crash.*.log

# Plan files and any state-bundle/export archives - these can contain
# secrets pulled from data sources (RDS passwords, etc.) just like state
# itself does, so they must never be committed either.
tfplan
tfplan*
*.tfplan
*.binary
*.tar
*.tar.gz
phoenix
