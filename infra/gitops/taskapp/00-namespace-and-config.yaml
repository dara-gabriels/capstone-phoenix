apiVersion: v1
kind: Namespace
data:
  FLASK_ENV: "production"
  POSTGRES_DB: "taskapp"        # keep — harmless, just unused by the app
  POSTGRES_USER: "taskapp_user" # keep — same
  DATABASE_HOST: "postgres-svc.taskapp.svc.cluster.local"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "taskapp"
  DATABASE_USER: "taskapp_user"
  CORS_ORIGINS: "https://taskapp.taskjob.name.ng"   # optional, tighten from default "*" when ready
kind: ConfigMap
metadata:
  name: taskapp-config
  namespace: taskapp